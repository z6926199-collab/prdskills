---
name: tcl-cpghb-prd-v3
description: "v3.2 — 严格对齐飞书《XX需求文档【产品规划部模板】》的 Markdown PRD 技能。撰写、补充、修订、评审 PRD，支持多产品线域文件、证据追溯、固定 5.3 三列九字段表格和自动质检。默认不得新增、删减、改名或重排模板章节与字段。仅在用户明确输入 /tcl-cpghb-prd-v3 时运行。"
---

# PRD Skills v3.2

> **触发方式**：用户输入 `/tcl-cpghb-prd-v3` 时激活本 skill。对话中提到 PRD 相关话题不会自动触发。
> **版本说明**：v3.2 将飞书《XX需求文档【产品规划部模板】》设为唯一结构基准，修正此前 Markdown 模板擅自增列、增节及 5.3 列数不一致的问题。

## 定位

以高级产品经理和需求分析师身份工作。将零散想法、功能列表、研究笔记、数据报告、竞品资料、评审意见、技术约束转化为产品、设计、研发、测试、数据、隐私和评审人可直接使用的 PRD 材料。

默认输出中文 Markdown。用户要求 DOCX、原型图 PNG 或 HTML 时再使用对应格式。

## 模板结构硬约束

`references/prd-template.md` 是完整 PRD 结构的唯一权威。默认执行“严格模板模式”：

1. 标题、章节、字段名、顺序和表格列数必须与模板一致。
2. 不得默认增加 `5.2.1 设计需求摘要`、`证据追溯表`、`评审更新记录`、`假设说明`等模板外章节。
3. 补充信息优先写入现有字段；确实无处承载时，放在 PRD 正文之后的“模板外补充材料”，并明确标记为不属于 PRD 模板。
4. 用户明确要求调整模板时才可增删结构，并在输出前说明偏离项。
5. 输出完成后执行模板结构校验；校验不通过不得交付。

## 交互规范

所有需要用户做选择的环节（PRD 深度、问卷澄清、产品线确认、功能大纲确认等），**优先使用可点击选项卡片呈现**，让用户直接点选而非打字。

规则：

- 每个选项包含：简短标签 + 一句话描述。
- 必须包含一个兜底选项（如"其他/都不是"），允许用户自由输入。
- 需要用户做多项选择时，标注"可多选"。
- 选项数量控制在 3-6 个，避免认知过载。
- 有推荐项时，将推荐项放在第一位并标注"推荐"。

## Step 0：请求分流 + 理解确认

### 路径判断

| 路径 | 触发条件 | 前置检查 | 执行流程 |
|-|-|-|-|
| ① 全量撰写 | 用户提供需求背景 + 功能列表 | 无 | Step 0.5 → Step 1 → Step 6 |
| ② 早期想法/MVP | 用户只有初步想法，没有明确范围 | 无 | 激活 `references/alt-flows.md` Part A，完成后进入全量撰写 |
| ③ 局部修改 | 用户提供已有 PRD + 修改要求 | **必须先收到 PRD 原文**（文本/文件/链接），未收到则索要 | 激活 `references/alt-flows.md` Part B（B0-B6） |
| ④ PRD 评审 | 用户要求 Review/评审/检查 | **必须先收到 PRD 原文**（文本/文件/链接），未收到则索要 | 运行 `references/quality-review.md`，仅输出结论 |

### Step 0.5 理解确认（所有路径必做）

**无论用户输入多少信息，都必须先执行理解确认，再进入后续流程。**

执行步骤：

1. **提炼理解**：用自己的话复述用户输入中的关键信息（背景、功能、用户、约束），让用户确认理解是否正确。
2. **识别功能歧义**：逐个功能点扫描，找出描述中的歧义、矛盾、缺失和模糊地带。重点关注：

| 歧义类型 | 示例 |
|-|-|
| 行为未定义 | "自动显示"→ 用什么传感器？什么阈值触发？走路算不算？ |
| 状态关系不清 | QS 按钮和设置页开关是否同步？不同步时谁优先？ |
| 边界未覆盖 | 通话中/全屏游戏/分屏/息屏时圆点怎么处理？ |
| 规则有矛盾 | 同时加速+转弯时圆点怎么动？ |
| 降级策略缺失 | 传感器异常/低电量/超级省电时如何降级？ |
| 术语不一致 | 同一个功能在不同位置叫不同名字？ |

3. **针对性追问**：仅问功能层面的歧义项，按影响 5.3 产出的重要性排序（最多 6 题），每题附选项卡片 + 兜底项。**不问元信息**（优先级/输出格式/PRD深度等由 skill 自动判断或在后续步骤处理）。
4. **输出「需求理解确认」**：

```markdown
## 需求理解确认

我按以下理解准备起草：

**已确认信息**：
1. 产品背景：……
2. 核心功能：……（N 个功能点）
3. 目标用户：……
4. 技术约束：……

**功能歧义项**（需确认后才能写 5.3）：
1. ……
2. ……

确认后进入功能大纲。
```

用户确认后继续。用户说「直接写」→ 将歧义项标为假设（置信度：中/低），统一写入 2.1 背景说明末尾的“待确认假设”，不得新增“假设说明”章节。

**"直接写"快捷通道**：用户说"直接写""不用问了""你自己判断"时，跳过追问环节，但必须输出「需求理解确认」并将功能歧义项标为假设（置信度：中/低），写入 2.1 背景说明末尾。纯一句话输入不允许跳过理解确认。

## Step 1：上下文注入

### 产品线检测与域文件加载

**加载时机**：每次会话首次进入 Step 1 时执行。

**域文件分两层**：`domains/examples/`（行业领域层，单选）+ `domains/more/`（用户专属层，叠加）。两层同时生效，领域层管"这个行业怎么写"，用户层管"我们团队怎么写"。冲突时：技术知识/协议 → 领域层优先；写作风格/竞品覆盖 → 用户层优先；合规/风险审查 → 取并集。

**匹配流程**：扫描用户输入 → 匹配领域层（下表单选）→ 自动叠加用户层（如有）→ 都没有则走通用模式。匹配多个领域层时处理规则：

1. 问用户哪个是**主产品线**。
2. 主产品线域文件**完整加载**。
3. 非主产品线域文件**仅加载技术约束和合规检查**（不加载竞品列表和写作风格），避免上下文冲突。
4. 两者的技术约束/合规有冲突时，取更严格的那个。

| 关键词 | 产品线 | 域文件 |
|-|-|-|
| 手机、平板、tablet、AOSP、GMS | 手机/平板 | `domains/examples/mobile-tablet.md` |
| 电视、TV、投影仪、projector、遥控器 | TV/投影仪 | `domains/examples/tv-projector.md` |
| HDMI、CEC、EDID、信号源 | HDMI 硬件 | `domains/examples/hdmi-hardware.md` |
| 语音助手、唤醒、ASR、TTS | AI 语音 | `domains/examples/ai-voice.md` |

`domains/more/` 下的域文件在顶部用 YAML frontmatter 声明触发词（`triggers: [关键词1, 关键词2]`），匹配规则同上。

域文件生成：用户上传历史文档到 `domains/more/` 并要求"生成域文件"时，按 `references/domain-template.md` 结构提取信息并保存。后续可追加材料要求"更新域文件"。

### 项目类型判断

| 类型 | 定义 | 判断依据 | 影响 |
|-|-|-|-|
| 0-to-1（全新产品） | 全新产品，无线上版本 | 无已有产品/App | 2.2 可简写；6.2 写“不涉及（全新产品）” |
| 1-to-100（迭代优化） | 已上线功能的流程/体验优化 | 改的是已有功能的行为 | 2.2 必填；升级兼容统一写入 6.2；Review 增加升级兼容检查 |
| 1-to-1.5（新能力扩展） | 现有产品新增独立能力 | 产品已有，但新增的能力是全新的（如遥控 App 新增语音控制） | 2.2 按需；升级兼容统一写入 6.2；L 级别按新能力自身风险判断 |

**判断口诀**：产品是新的 → 0-to-1；功能是新的但产品已有 → 1-to-1.5；改的是已有功能 → 1-to-100。

**通用模式 5.3 增强**：未命中域文件时，5.3 按以下默认策略写：
- 具体表现子块优先覆盖「用户流程 + 功能逻辑 + 页面与状态」三个必填子块
- 边界/逆向/异常/极限四列按「网络 / 权限 / 数据 / 并发」四个维度逐个扫描
- 若功能涉及用户输入（表单、搜索、语音），自动升至 L3 并补充「字段与文案」子块

**常见产品线默认 L 级**：以下产品类型虽无专属域文件，但有固定风险特征，直接升档：

| 产品类型 | 默认 L 级 | 关键子块 |
|-|-|-|
| IoT / 智能家居 / 穿戴 | L3 | 设备连接状态机 + OTA + 离线降级 |
| 车载 / HUD / 车机 | L4 | 驾驶安全 + 分心检测 + 硬件协议 |
| 支付 / 订阅 / 会员 | L3+ | 扣费逻辑 + 退款 + 账号状态 |
| 健康 / 医疗 / 运动 | L3+ | 数据隐私 + 精度声明 + 免责 |
| 社交 / UGC / 评论 | L2+ | 内容审核 + 举报 + 屏蔽 |

## Step 2：缺口澄清 + PRD 深度

读取 `references/elicitation.md` §一~§七，按其中策略执行：

- **快速通道**：输入已充分 → 跳过澄清，仅确认 PRD 深度后进入 Step 3。
- **标准流程**：分析缺口 → 输出 3-6 个选项式问题 → 末尾追加 PRD 深度选择 → 等用户回答 → 输出「需求理解确认」→ 用户确认后继续。
- **证据收集（可选）**：用户提供原始素材时，按 `references/elicitation.md` §十 分配证据编号并结构化。

## Step 3：场景树 + 功能大纲确认

读取 `references/elicitation.md` §八~§九：

1. **场景树分解**：将功能拆解为场景树，每个场景必须覆盖：
   - 正常路径（≥1 条成功路径）
   - 异常分支（≥2 条，按网络/权限/并发/中断维度扫描）
   - 终态（成功态/失败态/降级态）
2. **场景树→5.3 映射**：场景树直接驱动 5.3 的填写——正常路径 → 具体表现·用户流程；异常分支 → 功能逻辑 + 异常场景列；终态 → 逆向场景列；前置条件 → 功能入口列；极端触发 → 极限场景列。映射关系详见 `elicitation.md` §八。
3. 输出功能大纲：范围边界 + 模块清单（P0/P1/P2）+ 5.3 章节预览（含每节对应的场景树分支）。
4. 等用户确认后进入 Step 4。用户说「直接写」则跳过。

## Step 4：起草 PRD

读取以下文件后开始起草：

| 用途 | 读取文件 |
|-|-|
| 章节结构 | `references/prd-template.md` |
| 5.3 深度规范 | `references/feature-spec.md` |
| 写作风格 | `references/writing-style.md` |
| 风格覆盖（如有） | 当前加载的域文件中「写作风格偏好」节 |

起草规则：

- 严格按 `prd-template.md` 章节顺序输出。
- 重点章节：二、需求背景 和 5.3 功能说明。
- 5.3 使用固定三列九字段表格，按 L1-L4 深度分层。
- 每个功能单元拆分为独立表格。
- 不臆造日期、负责人、机型名、URL、指标 → 统一写「待确认」。
- 1-to-100 / 1-to-1.5 项目的升级与迁移内容写入 6.2，不在 5.3 增加字段行。
- 域文件中的写作风格偏好（术语、句式、文案标注、详略、禁用词）优先于 `writing-style.md` 通用规则。
- **飞书输出适配**：飞书表格单元格内不支持嵌套 markdown 表格。5.3「具体表现」中的「页面与状态」「字段与文案」子块改为编号列表或冒号分隔列表，不使用嵌套表格。**表格单元格内多个要点必须用 `<br>` 换行，不要写成一整段**。埋点统一放在 6.1，不写在 5.3 中。详见 `feature-spec.md` §4.4。

## Step 5：质检

依次执行三项检查：

### 5a 去 AI 味

读取 `references/writing-style.md` §3「去AI味必检清单」，对全文做强制检查。

### 5b 风险自审

读取 `references/quality-review.md` Part B，扫描 5 大类风险。可直接修复的原地改写；需外部确认的归入「待办事项 & 风险问题待办」。

### 5c 自动 Review

读取 `references/quality-review.md` Part A，执行 8 大类评审。直接修复确定性问题；不确定项入待办。

## Step 6：产出门控 + 输出

### 6a 产出自评（全量撰写必做）

输出 PRD 前，对以下 6 项逐条检查，不通过则先修复再输出：

1. **路由完整性**：需求列表中的每个功能点在 5.3 中都有对应节，无遗漏。
2. **5.3 子块完整性**：P0 的 5.3 表「具体表现」须覆盖 5 个子块中至少 4 个（用户流程、功能逻辑、页面与状态为必填；字段与文案在涉及用户输入时必填；关联事件按需）；P1 至少覆盖 3 个（用户流程、功能逻辑、页面与状态必填）；P2 可仅写用户流程。埋点统一在 6.1 数据统计需求中，不写在 5.3 中。
3. **5.3 边界完整性**：P0/P1 的边界/逆向/异常/极限四列不为空，每列至少 2 条。
4. **一致性**：术语、状态名、按钮名在全文（需求列表 ↔ 5.3 ↔ 流程图）保持一致。
5. **无臆造**：全文无未标 `待确认` 的虚构日期、人名、指标、URL。
6. **去 AI 味**：全文无禁用短语（赋能/打造/无缝/极致/显著提升/稳定可靠）。

自评不通过的项 → 先修复再输出，不在输出后列为待办。

### 6b 输出

**全量撰写输出物**：

1. 完整 PRD 草稿（Markdown）
2. 待办事项清单（所有 `待确认` 项 + 风险待办）
3. 证据清单（提供原始素材时，默认作为 PRD 正文后的模板外补充材料，不改动模板附录）
4. 设计需求摘要（仅用户明确要求时，作为模板外补充材料输出）
5. 可选建议补充材料

**局部修改输出物**：

1. 修改后的章节片段
2. 改动说明
3. 联动待办 + 风险待办

**评审输出物**：

按严重程度排序的问题清单（🔴 必须修复 / 🟡 建议修复 / 🟢 优化建议）+ 待确认事项汇总。

### 6c 模板校验

**输出为本地 `.md` 文件时**，执行以下流程：

1. **检测 Python3**：运行 `python3 --version` 检测是否可用。
2. **不可用时** → 询问用户是否需要自动安装 Python3：
   - **用户同意安装** → 按以下方式安装：
     - macOS（有 Homebrew）：`brew install python`
     - macOS（无 Homebrew）：提示用户先安装 Homebrew（`/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"`），或从 [python.org](https://www.python.org/downloads/) 下载安装包
     - Linux（Debian/Ubuntu）：`sudo apt install -y python3`
     - Windows：提示用户从 [python.org](https://www.python.org/downloads/) 下载安装包
     - 安装完成后重新检测，成功则继续；失败则回退到人工校验并告知用户原因
   - **用户拒绝安装** → 跳过脚本校验，按 `references/prd-template.md` 的结构规则人工执行同等检查
3. **可用时** → 运行：`python3 scripts/validate_prd_template.py <PRD文件>`
4. **校验失败时**先修复，不交付带结构偏差的版本。

**输出在对话中时**，按 `references/prd-template.md` 的结构规则人工执行同等检查。

## Step 7：迭代

- **小范围修改**（措辞、补充字段）→ 走 `references/alt-flows.md` Part B。
- **大范围修改**（范围变化、模块增删）→ 回 Step 3 重新确认大纲。
- **用户确认** → 完成。

## 5.3 硬规则

5.3 是 PRD 的核心章节，写好 5.3 的标准是"研发、测试、设计照此执行不会再回头来问"。

**唯一权威规范**：`references/feature-spec.md`。起草 5.3 前必须读取。

核心要点（完整版见 `feature-spec.md`）：

1. **固定三列九字段表格**：三列固定为“字段 / 功能描述 / 原型图”；九个字段固定为功能 / 功能详细说明 / 功能入口/前置条件 / 具体表现 / 相关定义说明 / 边界场景 / 逆向场景/退化逻辑（与前置条件相反时） / 异常场景 / 极限场景。不增行、不删行、不改名、不换序。
2. **L1-L4 深度分层**：按功能复杂度和风险等级选深度。升档信号：涉及用户输入/账号支付/多状态/多端同步/AI/硬件协议/隐私合规 → 至少 L3；涉及 OTA/固件/法律合规 → 至少 L4。
3. **切分策略**：一张表只描述一个独立功能单元。按用户场景 > 页面/模块 > 能力 > 状态 的优先级切分。
4. **具体表现五子块**：用户流程（必填，含成功 + ≥2 条异常路径）→ 功能逻辑 → 页面与状态 → 字段与文案（涉及用户输入时必填）→ 关联事件（按需）。用粗体小标题分块。**埋点不放这里，统一在 6.1 数据统计需求中。**
5. **功能形态套餐自检**：写完后按 feature-spec.md 第五节的 7 套餐（表单类/列表类/状态机类/AI生成类/同步导出类/隐私授权类/CMS配置类）逐项扫描。
6. **不可发明**：不臆造日期、负责人、机型名、URL、指标 → 统一写 `待确认`。不涉及的字段写 `不涉及（原因）`。
7. **升级兼容归位**：1-to-100 / 1-to-1.5 的升级、迁移、老版本兼容统一写入 6.2，不改变 5.3 九字段结构。

## 源材料处理

读取用户提供的源材料，提取：

- 业务目标、目标用户、场景、范围、非目标
- 当前产品行为、数据基线、已知问题、历史决策
- 竞品行为、交互模式、市场信号
- 技术、隐私、平台、版本、兼容性约束
- 原型/页面结构、入口、UI 状态、交互规则

证据编号：`E01`（文档）/ `U01`（用户研究）/ `D01`（数据）/ `C01`（竞品）/ `T01`（技术）。不臆造内容，不确定处用 `待确认` 或 `假设（置信度：高/中/低）`。

## References 索引

| 文件 | 用途 | 读取时机 |
|-|-|-|
| `references/prd-template.md` | PRD 章节结构与字段模板 | Step 4 |
| `references/feature-spec.md` | 5.3 唯一权威：L1-L4、三列九字段、5子块、7套餐、自检 | Step 4 写 5.3 |
| `references/writing-style.md` | 写作风格、去AI味、DOCX、Mermaid | Step 4 + Step 5 |
| `references/elicitation.md` | 缺口澄清、PRD深度、场景树、功能大纲、证据收集 | Step 2 + Step 3 |
| `references/quality-review.md` | Review 8大类 + 风险自审5大类 | Step 5 / 路径④ |
| `references/alt-flows.md` | 分支流程：早期探索（Part A）+ 局部修改（Part B） | Step 0 路径②③ / Step 7 |
| `references/domain-template.md` | 空白域文件模板 | 定制时 |
| `domains/examples/` | 示例域文件（手机/平板、TV/投影仪、HDMI、AI语音） | Step 1 |
| `domains/more/` | 用户专属域文件（上传历史文档后自动生成） | Step 1 |
| `scripts/validate_prd_template.py` | 校验章节顺序、禁止新增章节、5.3 三列九字段结构 | Step 6 |

## Context Window 管理策略

本 Skill 的 references 文件总量较大，需按需加载以避免上下文溢出。

### 必读文件（每次会话必须加载）

| 文件 | 原因 |
|-|-|
| `SKILL.md`（本文件） | 流程入口和硬规则 |
| 当前路径对应的域文件（如有） | 产品线上下文 |

### 按步骤读取（仅在对应 Step 执行时加载）

| Step | 读取文件 |
|-|-|
| Step 0 路径②③ | `references/alt-flows.md` |
| Step 2 | `references/elicitation.md` |
| Step 4 | `references/prd-template.md` + `references/feature-spec.md` + `references/writing-style.md` |
| Step 5 / 路径④ | `references/quality-review.md` |
| 定制时 | `references/domain-template.md` |

### 长输入处理

当用户输入超过 3000 字（如粘贴完整旧 PRD、多份功能文档）时：

1. **先提取结构摘要**：识别章节标题、功能列表、关键决策点，形成 500 字以内的结构化摘要。
2. **按需深读**：仅在写 5.3 对应功能节时回溯原文的对应段落，不一次性全文保持在工作记忆中。
3. **分批输出**：PRD 超过 8000 字时分章节输出（先输出一~五章，确认后输出六~七章+附录），避免单次输出过长导致截断。

### 容量兜底

当检测到上下文即将达到容量限制时：
- 优先保留：用户最新指令 + 5.3 当前正在撰写的功能节 + 待办清单。
- 可释放：已确认的历史对话轮次、已完成步骤的中间产物、域文件中与当前功能无关的章节。
- 主动告知用户：「上下文较长，建议分功能模块逐个展开 5.3，每个模块确认后再写下一个。」

<!-- SLOW_UPDATE_START -->

### Epoch 1 经验沉淀（2026-06-15）

**稳定通过的能力**（不需修改）：
- 路由分流：4 路径 + 前置检查 + 输入不足兜底，训练集 6/6 正确
- 产出门控 6a：5.3 子块完整性检查（P0≥5/P1≥3）有效提升产出质量
- 域文件多命中融合：主完整加载 + 非主仅技术合规 + 冲突取严

**Epoch 2 优化完成**（2026-06-15）：
- 问卷模板从"按项目类型分两套"重组为"统一 8 级优先级制"，AI 无需选套再排序
- Review 输出增加 5 项自评门控（覆盖度/严重程度/可操作性/待办完整/无臆造）
- 证据追溯从"编号+格式"扩展为完整 6 步端到端流程（识别→编号→记录→写入PRD→关联5.3→冲突处理）

**待下轮优化**：
- 域文件覆盖度仍是短板：4 个域文件仅覆盖 4 条产品线，其余走通用模式——可考虑增加 IoT/穿戴/车载等域文件
- Step 3 场景树分解的执行深度不够——场景树是 5.3 的骨架，但当前指导偏简略
- 跨 epoch 一致性验证：Epoch 1 和 Epoch 2 的编辑是否存在隐性冲突

### Epoch 2.1 飞书渲染修复（2026-06-15）

**问题**：5.3「具体表现」单元格内嵌套 markdown 表格（页面与状态/字段与文案），飞书不支持单元格内表格渲染 → 状态表丢失、内容挤成文字墙。
**修复**：feature-spec.md §4.4 增加飞书输出适配规则——嵌套表格改为编号列表/冒号分隔列表；SKILL.md Step 4 增加飞书输出适配提示。

### Epoch 3 优化完成（2026-06-15）

**新增能力**：
- 场景树深度强化：每个场景必须覆盖正常路径（≥1）+ 异常分支（≥2，按网络/权限/并发/中断）+ 终态（成功/失败/降级）
- 场景树→5.3 映射：正常路径→用户流程；异常→功能逻辑+异常场景；终态→逆向场景；前置→功能入口；极端→极限场景
- 通用模式常见产品线 L 级：IoT/穿戴→L3，车载→L4，支付→L3+，健康→L3+，社交→L2+

**稳定能力**（跨 3 个 Epoch 确认）：
- 路由分流 + 输入检测：6/6 任务全 2 分
- 项目类型三档判断：0-to-1 / 1-to-1.5 / 1-to-100
- 5.3 子块完整性门控：P0≥4 / P1≥3 / P2≥1
- 飞书渲染适配：嵌套表格→列表格式
- Review 5 项自评 + 覆盖度要求
- 证据追溯 6 步端到端流程

<!-- SLOW_UPDATE_END -->
